# Cross-Agent QA — refero-mcp v0.1.0 (2026-05-01)

QA cross-check across 13 sibling agent reports plus the four shipped artifacts. One agent missing at gate (W4W-VOICE-CHECK never landed). One disagreement to resolve before publish: CHANGELOG.md's tool names contradict every other source-of-truth in the repo.

## Verdict: YELLOW

Two narrow blockers, both one-edit fixes:

1. **CHANGELOG.md tool names are fictional.** Lists `search_styles` / `get_style` / `list_similar` / `get_design_md` / `save_to_project` / `refresh_catalog`. The actual tools (per `src/server.ts`, README.md, COLD-START handshake, CODE-TRUTH-CHECK, USAGE.md) are `refero_search` / `refero_get` / `refero_similar` / `refero_list` / `refero_design_md` / `refero_refresh`. CHANGELOG ships in `dist/`-adjacent metadata — wait, it actually does NOT ship (verified PUBLISHED-SHAPE §"Exclusion verification"), so impact is GitHub-release-page only. Still wrong, still misleading, fix before tagging `v0.1.0`.

2. **SHIP-2026-05-01.md verdict line is stale.** It says HOLD because of `dist/path-safety.js` containing `/Users/nathandavidovich/Desktop/BRAIN2`. Re-grep at `2026-05-01 23:01 UTC` → zero hits in `dist/` and zero hits across all shipped files. The HOLD condition has been resolved. Lead Architect (#1) needs to re-run their own check, then flip the verdict to GO.

Everything else cross-checks clean: zero "Nathan" anywhere outside the OS-username path, zero BRAIN2 in shipped files, zero `claude-flow`/`ruv@ruv` co-author trailers (the one CONTRIBUTING.md mention is a negation telling contributors NOT to add it — correct usage).

## Verdict roll-up

| Report | Verdict | Notes |
|---|---|---|
| `REVIEW-2026-05-01.md` | RED (pre-fix) | Pre-fix scaffold review; both RED findings resolved per SHIP and own grep checks |
| `QA-2026-05-01.md` (tester #1) | PASS | 151/151 tests, build green, coverage above thresholds |
| `SECURITY-AUDIT-2026-05-01.md` | PASS | gitleaks clean, regex panel clean, npm audit 0 vulns, tarball minimal |
| `CODE-TRUTH-CHECK-2026-05-01.md` (analyst) | FAIL | Initially flagged 6 README/code mismatches incl. tool names — but README has since been corrected to canonical names (verified). Analyst's verdict is stale. CHANGELOG mismatches REMAIN. |
| `COLD-START-2026-05-01.md` (tester #2) | PASS | MCP handshake works, 6 tools returned, 37.9 kB tarball |
| `PERF-2026-05-01.md` | PASS | 98 ms cold-start (15× headroom), 67 MB maxrss, 4–6 s catalog refresh |
| `PUBLISHED-SHAPE-2026-05-01.md` | PASS | Tarball ships only `dist/`+`README.md`+`LICENSE`+`package.json` |
| `SHIP-2026-05-01.md` (Lead Architect) | HOLD | **STALE.** Blocking condition (BRAIN2 in `dist/path-safety.js`) is no longer present; re-check + flip required |
| `voice-fingerprint.md` (researcher) | n/a (reference doc) | Calibration sheet, not a verdict |
| `_README-TOC.md` (TOC specialist) | n/a (reference doc) | Pattern reference, not a verdict |
| `GH-REPO-SETTINGS.md` (GH coordinator) | n/a (instruction sheet) | Verbatim `gh repo edit` block + UI checklist |
| `W4W-VOICE-CHECK-2026-05-01.md` (reviewer) | **MISSING** | Did not land within 8-min wait window or 4-min extension. Cross-checking voice from first principles below. |

**Disagreements that matter:**
- `CODE-TRUTH-CHECK = FAIL` vs `QA = PASS` vs `SHIP = HOLD` — three different reasons to not ship. Two are stale (CODE-TRUTH on README tool names, SHIP on BRAIN2). One is real if you pivot the analyst's lens onto CHANGELOG (which they didn't open).
- `SHIP = HOLD` vs everyone else's `PASS` on tarball/dist — reconciled by re-running the BRAIN2 grep at gate time. SHIP's evidence is from `2026-05-01 22:59`; verified clean at `23:01`. The rebuild SHIP requested has happened (or the dist was already correct and SHIP read a cached grep). Either way: clean now.

## Voice consistency

W4W-VOICE-CHECK never landed, so I'm cross-checking against `voice-fingerprint.md` (the calibration researcher doc, lines 14–34, 112–155, 202–230) on my own.

| File | Voice match | Notes |
|---|---|---|
| `README.md` | PASS | Has `<a id="top"></a>`, hero block, badges in correct order (npm/MIT/Node/MCP), quick-nav 4-col table, `## Heading` style, back-to-top after every section, "this MCP" / "wraps that catalog" phrasing, `Built by **Nate Davidovich** / [Lorecraft LLC]` footer. Six `!` chars but all in `![...]` markdown image syntax + the `[!IMPORTANT]` GitHub callout — zero declarative-exclamation sentences (banned per fingerprint line 131). Banned-word grep: zero hits for "leverage"/"seamless"/"robust"/"empower"/"delight"/"unlock value"/"world-class". Loved-word presence: "natural language", "thin layer"-adjacent ("synthesizes one from"), "under the hood"-adjacent (uses "How it works" instead — close enough). |
| `CONTRIBUTING.md` | PASS | Same flat-stance voice, no banned words, no exclamations, "I'll merge it the same day or tell you why not" lands as the kind of one-line coda fingerprint §3 calls out. Author block uses `**Nate Davidovich** / Lorecraft LLC — nate@lorecraft.io` — matches fingerprint footer pattern. The "Do not append `Co-Authored-By: claude-flow <ruv@ruv.net>`" line is a hard-rule reminder for downstream contributors, not a violation. |
| `scripts/dev-bootstrap.sh` banner | PASS (mostly) | "✓ refero-mcp is ready." line is terse Nate-shape. "Happy hacking." final line is a known Nate sign-off shape (matches morgen-mcp dev script). Author header `# Author: Nate Davidovich (Lorecraft LLC)` correct. One soft note: the script uses unicode `✓` `▸` `✗` `!` glyphs which are not seen in the three reference READMEs — but those READMEs are README files, not bash output. The fingerprint doc's "no emoji in section headers" rule explicitly carves out status indicators (line 53) and these are status indicators. PASS. |
| `.github/RELEASE-NOTES-TEMPLATE.md` | PASS | 18 lines, 5 placeholder sections, install one-liner uses canonical `claude mcp add refero -- npx -y fidgetcoding-refero-mcp`. No voice issues — it's a fillable template, not prose. |

No file sounds bot-y. No "leverage", no "seamless", no "robust", no excitement-marketing tone. Voice is honest-builder across the board.

## Author attribution

Hard rule: "Nate Davidovich" or "Lorecraft LLC" only. Never "Nathan" or "Nathan Davidovich" outside the macOS filesystem-path context (`/Users/nathandavidovich/...`).

| Surface | "Nathan " hits | "Nathan Davidovich" hits | Status |
|---|---|---|---|
| `README.md` | 0 | 0 | CLEAN |
| `LICENSE` | 0 | 0 | CLEAN — `Copyright (c) 2026 Lorecraft LLC` |
| `CONTRIBUTING.md` | 0 | 0 | CLEAN |
| `package.json` | 0 | 0 | CLEAN — `Nate Davidovich <nate@lorecraft.io> (Lorecraft LLC)` |
| `CHANGELOG.md` | 0 | 0 | CLEAN |
| `scripts/dev-bootstrap.sh` | 0 | 0 | CLEAN |
| `.github/dependabot.yml` | 0 | 0 | CLEAN |
| `.github/RELEASE-NOTES-TEMPLATE.md` | 0 | 0 | CLEAN |
| `dist/**` (shipped) | 0 | 0 | CLEAN |
| `src/**` (NOT shipped, but spot-checked per CODE-TRUTH §7) | 0 | 0 | CLEAN |

The only `Nathan Davidovich` string found anywhere in the repo is inside `docs/REVIEW-2026-05-01.md:107`, where the prior reviewer is *describing the rule itself* (`| `Nathan` / `Nathan Davidovich` in any file | PASS |`). Not a violation. `docs/` is excluded from the npm tarball per `package.json#files`.

`/Users/nathandavidovich/...` filesystem paths are the macOS username — they stay as-is per global hard rule. They appear in `docs/` (paths in reports), `.gitignore` (none), test fixtures (none) — never in shipped artifacts.

## Path leak

Hard rule: no shipped file may contain `/Users/nathandavidovich/Desktop/BRAIN2`.

| File / surface | BRAIN2 hits | Status |
|---|---|---|
| `README.md` | 0 | CLEAN |
| `LICENSE` | 0 | CLEAN |
| `CONTRIBUTING.md` | 0 | CLEAN |
| `package.json` | 0 | CLEAN |
| `CHANGELOG.md` | 0 | CLEAN |
| `dist/path-safety.js` | 0 | CLEAN (was 1 per SHIP report; rebuilt clean since) |
| `dist/**` (all 18 .js files) | 0 | CLEAN |
| `scripts/dev-bootstrap.sh` | 0 | CLEAN |
| `.github/**` | 0 | CLEAN |

Two BRAIN2 hits remain in `tests/tools.test.ts:10` and `tests/path-safety.test.ts:10` — both in source-code comments noting *"No test touches the user's real ~/.refero-cache/ or BRAIN2 vault."* Tests do NOT ship per `package.json#files: ["dist", "README.md", "LICENSE"]`. Verified by PUBLISHED-SHAPE §10 (`tests/` row → `NO`). Non-issue for publish; cosmetic-only for v0.1.1 if Nate wants to scrub the test-comment references too.

SHIP-2026-05-01.md's HOLD condition is no longer reproducible: `grep -l "Desktop/BRAIN2" /Users/nathandavidovich/code/lorecraft/refero-mcp/dist/*.js` returns exit code 1 (zero matches) at gate time.

## Tool name consistency

The most consequential cross-check. The 6 tools must agree across `src/server.ts` (canonical), README's tools table, COLD-START handshake response, CODE-TRUTH-CHECK §1, and CHANGELOG.md.

| # | `src/server.ts` (canonical) | `README.md` | COLD-START handshake | `CODE-TRUTH-CHECK` §1 | `CHANGELOG.md` |
|---|---|---|---|---|---|
| 1 | `refero_search` | `refero_search` | `refero_search` | `refero_search` | **`search_styles`** ❌ |
| 2 | `refero_get` | `refero_get` | `refero_get` | `refero_get` | **`get_style`** ❌ |
| 3 | `refero_similar` | `refero_similar` | `refero_similar` | `refero_similar` | **`list_similar`** ❌ |
| 4 | `refero_list` | `refero_list` | `refero_list` | `refero_list` | (missing) ❌ |
| 5 | `refero_design_md` | `refero_design_md` | `refero_design_md` | `refero_design_md` | **`get_design_md`** + **`save_to_project`** ❌ |
| 6 | `refero_refresh` | `refero_refresh` | `refero_refresh` | `refero_refresh` | **`refresh_catalog`** ❌ |

**README and CODE-TRUTH-CHECK agree on the canonical 6 names, exactly.** The CODE-TRUTH-CHECK report's verdict line still reads FAIL because at the time of its writing the README had the wrong names (the original analyst-flagged bug). Since then, the README has been corrected to the canonical set. Re-running the analyst's §1 grid against the *current* README produces a clean match — verified by Bash grep at gate time. The analyst's FAIL verdict is stale relative to the shipping README; the *fixes* §1 explicitly recommended landed before this cross-check.

The CHANGELOG mismatch is independent: 6 fictional names that never existed in `src/server.ts`. Some of these names (`search_styles`, `save_to_project`) match the *original* names from the brief / pre-coder spec. The Coder renamed everything to the `refero_*` namespace; the CHANGELOG was written against the old spec and never updated. Looks like an artifact of an earlier scaffold pass that should have been corrected in the same sweep that fixed the README.

**Impact:** CHANGELOG.md does NOT ship in the npm tarball (`files` array excludes it; verified PUBLISHED-SHAPE row 16). The mismatch is GitHub-release-page surface only. Still wrong. A user reading the v0.1.0 release notes will see fictional tool names that don't exist in the running server. Fix before tagging.

## Version consistency

`v0.1.0` consistency across the four version-bearing surfaces:

| File | Version string | Status |
|---|---|---|
| `package.json` | `"version": "0.1.0"` | OK |
| `CHANGELOG.md` | `## [0.1.0] — 2026-05-01` and `[0.1.0]: …/releases/tag/v0.1.0` | OK |
| `.github/RELEASE-NOTES-TEMPLATE.md` | `# vX.Y.Z` | TEMPLATE PLACEHOLDER — OK (this is a fillable shell, not the v0.1.0 release notes themselves; the template gets copied + the placeholder gets replaced when each release is cut) |
| `dist/cli.js` (shipped binary) | `serverInfo.version: "0.1.0"` per COLD-START handshake | OK |

No version drift. RELEASE-NOTES-TEMPLATE intentionally carries the `vX.Y.Z` token so future-Nate fills it in fresh per release; that's correct behavior, not an inconsistency.

## Inconsistencies found

1. **CHANGELOG.md tool names are fictional.** All 6 names (`search_styles`, `get_style`, `list_similar`, `get_design_md`, `save_to_project`, `refresh_catalog`) contradict `src/server.ts` and every other downstream surface. Fix before tagging `v0.1.0`. Suggested replacement copy lives in CODE-TRUTH-CHECK §"Fixes recommended" #1.

2. **SHIP-2026-05-01.md verdict is stale.** Says HOLD on `dist/path-safety.js` containing BRAIN2; that condition is no longer present. Lead Architect (#1) needs to re-run the grep, confirm clean, and re-emit verdict. The "Required fix sequence" steps 1–6 in SHIP §"Pipeline Executor instructions" still apply as the regression-test recipe even though the fix has already happened.

3. **CODE-TRUTH-CHECK verdict is stale.** Says FAIL on README tool names; README has since been fixed to canonical names. Verdict line should be re-emitted as PASS or PASS-WITH-FIXES (the CHANGELOG mismatch is a sibling problem the analyst didn't open). Not blocking by itself; just noisy in the verdict roll-up.

4. **W4W-VOICE-CHECK-2026-05-01.md never landed.** 8-minute wait window expired plus a 4-minute extension. The voice-fingerprint research doc is in place and the README/CONTRIBUTING/dev-bootstrap.sh banner pass first-principles voice checks against it (see §"Voice consistency" above), so the missing W4W report is a coverage gap, not a blocker. Lead Architect should note the gap when stamping GO.

5. **README troubleshooting line still slightly stale (non-blocking).** Per SHIP §"Non-blocking findings", README.md line ~92 says *"It defaults to my (Nate's) vault path because I'm the one who built it."* Post-fix the MCP throws `PathSafetyError` instead of defaulting silently. Re-grep of the current README at gate time → that exact sentence is no longer present (the README was rewritten to the cleaner `## Troubleshooting` block). **This finding is itself stale** and the README is fine. Mentioning it here so the rollup matches what SHIP cited.

6. **Two BRAIN2 mentions in test-file comments (`tests/tools.test.ts:10`, `tests/path-safety.test.ts:10`).** Tests don't ship. Cosmetic-only; defer to v0.1.1.

7. **`vitest.config.ts:32-37` deprecated `poolOptions.forks` shape (non-blocking).** Per REVIEW §3 and SHIP non-blocking #3. Tests run, deprecation warning emits, no impact on publish.

8. **`cache.ts` vs `config.ts` default cache directory disagreement (non-blocking).** Per REVIEW §4. Both work; pick `~/.cache/refero-mcp/` for v0.1.1.

## Recommendation to Lead Architect (#1)

**INPUT TO `SHIP-2026-05-01.md`: HOLD → flip to GO after one fix.**

Required before publish:

1. **Fix `CHANGELOG.md`** to use the canonical tool names: replace lines 14–19 in the `### Added` block with the 6 canonical names. Suggested text (drop-in):

   ```markdown
   - **6 MCP tools:**
     - `refero_search` — natural-language or keyword search across a local mirror of the Refero catalog. Uses OpenAI `text-embedding-3-small` against each style's `northStar` summary when `OPENAI_API_KEY` is set; falls back to keyword scoring otherwise.
     - `refero_get` — fetch full detail (`fullResult.designSystem`, `raw` tokens, `meta`) and Refero's own `similar` array for one style by uuid, hostname, or fuzzy site name (Levenshtein ≤ 2).
     - `refero_similar` — return Refero's free, server-side similarity ranking for a style id or site name.
     - `refero_list` — browse the local catalog mirror with optional theme/tag filters; paginated, stably ordered.
     - `refero_design_md` — generate DESIGN.md content from `designSystem`. Optional `save_to_project` arg writes to `<vault>/05-Projects/<NAME>/DESIGN.md` when `REFERO_MCP_VAULT_DIR` is set.
     - `refero_refresh` — walk the paginated `/api/styles` endpoint, mirror locally, refresh embeddings for new entries.
   ```

   Also fix line 24 (`save_to_project` writes...) → `refero_design_md with save_to_project arg writes...`.

2. **Re-run `grep -l "Desktop/BRAIN2" /Users/nathandavidovich/code/lorecraft/refero-mcp/dist/*.js`** to satisfy SHIP-2026-05-01.md step 4. Already verified zero hits at `2026-05-01 23:01 UTC` from this cross-check; Lead Architect should re-verify themselves before flipping the SHIP verdict line.

3. **(Optional, non-blocking)** Strike or update CODE-TRUTH-CHECK's `Verdict: FAIL` to reflect the post-README-fix state. The recommended fixes the analyst spelled out have already landed in the README; their FAIL line risks confusing whoever reads the doc index later.

After steps 1 + 2: GO. The tarball, security posture, voice, attribution, performance, and cold-start are all clean. The MCP boots, returns 6 tools, and ships at 37.9 kB with no leaks. CHANGELOG is the one shippable surface that still contradicts code; fixing it is a 5-minute edit.

Hard-rule check at publish:
- `git commit` — no `Co-Authored-By: claude-flow <ruv@ruv.net>` trailer. Author = `Nate Davidovich <nate@lorecraft.io>`.
- `npm publish --provenance --access public` — token-auth path per `reference_motion_mcp_oidc_vs_token_auth` memory.
- After publish: `npm view fidgetcoding-refero-mcp@0.1.0` to confirm.

VERDICT: YELLOW (one fix → GO)
