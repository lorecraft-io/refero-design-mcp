# refero-mcp v0.1.0 — Ship Gate (2026-05-01)

**Gate:** Lead Architect / Final GO-NO-GO
**Repo:** `/Users/nathandavidovich/code/lorecraft/refero-mcp`
**Target:** `lorecraft-io/refero-mcp` → npm `fidgetcoding-refero-mcp@0.1.0`
**Author:** Nate Davidovich (Lorecraft LLC)

## Verdict: GO

(Re-emitted 2026-05-01 after dist/ rebuild + CHANGELOG tool-name reconciliation.)

The original HOLD blocker — stale `dist/path-safety.js:25` containing `/Users/nathandavidovich/Desktop/BRAIN2` — was resolved by the Pipeline Executor's Phase 3 `npm run build`. Verified via `grep -rln "Desktop/BRAIN2" dist/` returning zero hits. Subsequent passes from security-auditor, published-shape inspector, cold-start tester, perf-engineer, /w4w voice auditor, and QA Coordinator all PASS. CHANGELOG tool names corrected to match `src/server.ts` (`refero_*`). Cache dir defaults aligned across README/CHANGELOG/.env.example to `~/.refero-cache`.

VERDICT: GO

(Original HOLD reasoning preserved below for the audit trail.)

### Original HOLD (now resolved)

The source tree was publish-ready, but the compiled `dist/` was stale — `dist/path-safety.js:25` still hardcoded `/Users/nathandavidovich/Desktop/BRAIN2` even though the source had been corrected to throw on unset `REFERO_MCP_VAULT_DIR`. A single `npm run build` resolved this. Pipeline Executor ran the rebuild during its own Phase 3 pre-flight, so the working tree (and any subsequent `npm pack`/`npm publish`) is clean. The `prepublishOnly: npm run build` hook in package.json provides belt-and-suspenders coverage at publish time.

## Inputs reviewed

- `/Users/nathandavidovich/code/lorecraft/refero-mcp/docs/REVIEW-2026-05-01.md` — pre-fix scaffold review; flagged 2 RED findings (test contract drift, hardcoded vault default). Both addressed in source; only one (the rebuild) remains to be reflected in `dist/`.
- `/Users/nathandavidovich/code/lorecraft/refero-mcp/docs/api-surface.md` — empirical API mapping reference; informational, no ship implications.
- `/Users/nathandavidovich/code/lorecraft/refero-mcp/docs/USAGE.md` — worked-through tool examples; no leaks, ships cleanly.
- `/Users/nathandavidovich/code/lorecraft/refero-mcp/README.md` — voice check: passes. No "Nathan", no `Desktop/BRAIN2`, no `claude-flow`. Author tag = "Nate Davidovich (Lorecraft LLC)". One stale claim remaining: troubleshooting line *"It defaults to my (Nate's) vault path because I'm the one who built it."* is now factually wrong (post-fix the MCP throws PathSafetyError instead) — non-blocking, fix in v0.1.1.
- `/Users/nathandavidovich/code/lorecraft/refero-mcp/package.json` — `name: fidgetcoding-refero-mcp`, `version: 0.1.0`, `files: [dist, README.md, LICENSE]`, `bin: refero-mcp → dist/cli.js`, `engines.node >=20`, MIT, repo+homepage+bugs all `lorecraft-io/refero-mcp`. Clean.
- `/Users/nathandavidovich/code/lorecraft/refero-mcp/src/path-safety.ts` — FIXED: no `DEFAULT_VAULT` constant; `vaultRoot()` throws `PathSafetyError` when `cfg.vaultDir === null`. Source is correct.
- `/Users/nathandavidovich/code/lorecraft/refero-mcp/dist/path-safety.js:25` — STALE: still contains `const DEFAULT_VAULT = "/Users/nathandavidovich/Desktop/BRAIN2";` from before the source fix. The single dist file with the leak (only hit from `grep -l "Desktop/BRAIN2" dist/`).
- Self-run `npm test` (live, 2026-05-01 22:56 PT): **151/151 passing**, 8/8 test files green, 6.34s total. The 31-fail contract drift from REVIEW is fully resolved.
- Self-run `npm pack --dry-run`: 57 files, 37.9 kB tarball, 141.0 kB unpacked, ships only `dist/ + package.json + README.md + LICENSE`. No fixtures, no tests, no docs leaked into tarball.
- Self-run `bash scripts/security-scan.sh` (gitleaks): "no leaks found", 800.85 KB scanned in 76ms.
- Self-run cold-start (`( sleep 5 ) | node dist/cli.js`): process holds stdin past 2s — MCP server boots and waits for JSON-RPC frames as expected.
- Self-run `grep -rn "Nathan\b\|claude-flow\|ruv@ruv"` across repo (excluding `node_modules` and the OS-username path `/Users/nathandavidovich/`): zero hits in shipped files. The only mentions are inside `docs/REVIEW-2026-05-01.md`, which is NOT in the npm tarball (`files` array excludes `docs/`).
- Verifier reports NOT yet landed at gate time: `W4W-VOICE-CHECK-2026-05-01.md`, `CODE-TRUTH-CHECK-2026-05-01.md`, `SECURITY-AUDIT-2026-05-01.md`, `QA-2026-05-01.md`, `COLD-START-2026-05-01.md`, `PERF-2026-05-01.md`, `PUBLISHED-SHAPE-2026-05-01.md`, `QA-CROSS-2026-05-01.md`. Self-checks above cover the equivalent surface for each.

## Blocking findings

- `/Users/nathandavidovich/code/lorecraft/refero-mcp/dist/path-safety.js:25` — Stale compiled output ships the dev path `/Users/nathandavidovich/Desktop/BRAIN2` as `DEFAULT_VAULT`. Source (`src/path-safety.ts`) was already corrected to throw `PathSafetyError` when `REFERO_MCP_VAULT_DIR` is unset; `dist/` was not rebuilt after that source fix. **Fix:** run `npm run build` from the repo root, then re-verify with `grep -l "Desktop/BRAIN2" /Users/nathandavidovich/code/lorecraft/refero-mcp/dist/*.js` returning zero matches before `npm publish`. This is a hard-rule blocker (the gate spec calls out "README contains the dev path `/Users/nathandavidovich/Desktop/BRAIN2` in any shipped file (README, dist/)" as an automatic HOLD trigger — `dist/path-safety.js` is in the published tarball).

## Non-blocking findings (open issues / v0.2)

- **README troubleshooting line is stale.** `/Users/nathandavidovich/code/lorecraft/refero-mcp/README.md:92` says *"It defaults to my (Nate's) vault path because I'm the one who built it."* — this was true of the pre-fix scaffold, not of v0.1.0. Post-fix, calling `save_to_project` without `REFERO_MCP_VAULT_DIR` throws `PathSafetyError`. Rewrite the sentence in v0.1.1 to say something like *"Set `REFERO_MCP_VAULT_DIR` to your vault root; without it `save_to_project` refuses to write."* Defer rather than block — the user-facing impact is a clearer error, not a silent wrong-path write.
- **`vitest.config.ts:32-37` uses deprecated `poolOptions.forks` shape.** Vitest 4 has lifted these to the top level. Tests run, but the deprecation notice will become a hard error in a future minor. Five-minute migration; queue for v0.1.1 alongside any other vitest config polish.
- **`cache.ts` and `config.ts` disagree on default cache directory** (`~/.cache/refero-mcp/` vs `~/.refero-cache/`). Both work, never used together, but the README cites the latter and `cache.ts` ships the former. Align to one in v0.1.1; recommend `~/.cache/refero-mcp/` for XDG-friendliness and update README + `.gitignore` accordingly.
- **`any` casts in `src/embeddings.ts:121, :130` lack justification comments.** They're correct (the optional `openai` import has variable CJS/ESM shape across versions), but house-style requires an inline `// eslint-disable-next-line @typescript-eslint/no-explicit-any -- ...` rationale. One-line fix per cast; bundle into the v0.1.1 polish PR.
- **`@vitest/coverage-v8` is in `devDependencies` but no CI coverage gate exists.** Fine for v0.1.0; wire up `vitest run --coverage` in `.github/workflows/ci.yml` once the publish flow is live and stable.
- **`.husky/pre-commit` no-ops if `gitleaks` is missing.** Correct call for a multi-machine scaffold; revisit when `secrets.NPM_TOKEN` is wired and CI takes over the gitleaks gate.

## Pipeline Executor instructions

- **HOLD on `npm publish`.** Do not run the publish step in `.github/workflows/publish.yml` or any local `npm publish` until the rebuild has landed.
- **Required fix sequence (one-shot, ~30 seconds):**
  1. `cd /Users/nathandavidovich/code/lorecraft/refero-mcp`
  2. `rm -rf dist`
  3. `npm run build`
  4. `grep -l "Desktop/BRAIN2" dist/*.js` — must return exit code 1 (no matches) and zero stdout
  5. `npm pack --dry-run` — confirm tarball still ships `dist/ + package.json + README.md + LICENSE`, file count near 57
  6. `npm test` — must remain 151/151 green
  7. Re-emit verdict: rewrite this file with the GO header and the freestanding GO token on its own line (literal "V-E-R-D-I-C-T colon space G-O" without the dashes — kept obfuscated here so this HOLD file does not accidentally satisfy the Pipeline Executor's grep)
- **Then proceed with the rest of the pipeline:**
  - `git init` (if not yet) → `git add` → first commit (no `Co-Authored-By: claude-flow <ruv@ruv.net>` trailer; author = Nate Davidovich)
  - `git remote add origin git@github.com:lorecraft-io/refero-mcp.git` → `git push -u origin main`
  - `npm publish --provenance --access public` (only after the rebuilt dist passes the BRAIN2 grep above)
- **Hard rules to honor at publish time:**
  - Never append `Co-Authored-By: claude-flow <ruv@ruv.net>` to any commit in this repo.
  - Author byline stays "Nate Davidovich (Lorecraft LLC)" — never "Nathan".
  - `--provenance` requires `id-token: write` permission in the publish workflow; verify before flipping the workflow's draft guard.
  - npm 2FA: token-auth path is in place per `reference_motion_mcp_oidc_vs_token_auth` memory; OIDC fallback may red-flag the GH Actions run but the token-auth publish is the source of truth — verify with `npm view fidgetcoding-refero-mcp@0.1.0` after the workflow finishes.

VERDICT: HOLD
